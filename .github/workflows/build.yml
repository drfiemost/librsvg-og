name: C/C++ CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: deps
      run: sudo apt-get install gtk-doc-tools gobject-introspection libcroco3-dev libpango1.0-dev libgtk-3-dev libgdk-pixbuf2.0-0
    - uses: actions/checkout@v3
    - name: bootstrap
      run: ./autogen.sh
    - name: configure
      run: export GDK_PIXBUF_QUERYLOADERS="/usr/lib/x86_64-linux-gnu/gdk-pixbuf-2.0/gdk-pixbuf-query-loaders" ./configure
    - name: make
      run: make
    - name: make distcheck
      run: make distcheck
